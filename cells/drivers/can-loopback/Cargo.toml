[package]
name = "driver-can-loopback"
version = "0.1.0"
edition = "2021"

[dependencies]
hal-can = { path = "../../../hal/traits/can" }

[build-dependencies]
cell-build = { path = "../../../libs/cell-build" }
