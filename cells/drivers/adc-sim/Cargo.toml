[package]
name = "driver-adc-sim"
version = "0.1.0"
edition = "2021"

[dependencies]
hal-adc = { path = "../../../hal/traits/adc" }

[build-dependencies]
cell-build = { path = "../../../libs/cell-build" }
