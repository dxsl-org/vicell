[package]
name = "driver-gpu"
version = "0.1.0"
edition = "2021"

[dependencies]
types = { path = "../../../libs/types" }
api = { path = "../../../libs/api" }
ostd = { path = "../../../libs/ostd" }

[features]
default = []

[lib]
crate-type = ["staticlib"]

[build-dependencies]
cell-build = { path = "../../../libs/cell-build" }
